package tehnut.quest.api;

public enum QuestType {

    /** Gather X amount of Y material(s) and return to board. */
    GATHER(),
    /** Travel to provided coordinates. */
    TRAVEL(),
    /** *shrug* */
    RETRIEVE(),
    /** Slay X amount of Y monster(s) and return to board. */
    SLAY(),
    ;
}
